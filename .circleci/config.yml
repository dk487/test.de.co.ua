version: 2.1

workflows:
  default-workflow:
    jobs:
      - jekyll-build

jobs:
  jekyll-build:
    docker:
      - image: jekyll/jekyll
    steps:
      - checkout
      - run:
          name: "Build the site"
          command: "pwd; ls; cd ~/project; pwd; ls; jekyll build"

